# Local-only backups of the original full-resolution images (not for deploy)
_backups/
New Pictures/
SS Badger CW Vans.jpg
*.zip

# Node deps (installed by Netlify at build time)
node_modules/

# OS / editor noise
.DS_Store
Thumbs.db

# Raw camera originals: converted versions live in colorwagonrentals/
*.HEIC
_sheet/

# Netlify CLI scratch (created by `netlify dev`)
.netlify
deno.lock
